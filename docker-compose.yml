version: "3.8"

services:
  app:
    build: ./
    volumes:
      - ./src:/var/www/html:z
    ports:
      - 3000:80
    depends_on:
      - db
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
  pma:
    image: phpmyadmin
    restart: always
    environment:
      PMA_USER: root
      PMA_PASSWORD: rootpassword
    ports:
      - 3001:80
    depends_on:
      - db